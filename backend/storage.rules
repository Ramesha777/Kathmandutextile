rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Damage report images: authenticated users can read and write
    match /problem_report_images/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
